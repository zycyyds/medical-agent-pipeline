# Memory Agent 依赖

# 核心依赖
agentscope>=0.1.0  # Agent 框架（与 First Agent 保持一致）
pydantic>=2.0.0

# 可视化界面
streamlit>=1.28.0  # Web 界面

# 可选：Ollama Python 客户端（如果不使用 agentscope）
# ollama>=0.1.0

# 开发依赖
pytest>=7.0.0  # 测试框架
pytest-asyncio>=0.21.0  # 异步测试支持
