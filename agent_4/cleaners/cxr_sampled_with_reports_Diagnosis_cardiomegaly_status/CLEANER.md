---
name: cxr_sampled_with_reports_Diagnosis_cardiomegaly_status
description: 仅清洗 `cxr_sampled_with_reports_Diagnosis_cardiomegaly_status` 列
---
1. 仅读取并处理 `cxr_sampled_with_reports_Diagnosis_cardiomegaly_status`，其他列保持不变。
2. 统一空格、大小写与常见拼写差异，并仅对明确同义表达做轻量规范化；不确定时保留原值。