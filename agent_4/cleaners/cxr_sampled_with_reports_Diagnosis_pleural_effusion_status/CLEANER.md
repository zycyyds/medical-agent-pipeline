---
name: cxr_sampled_with_reports_Diagnosis_pleural_effusion_status
description: 仅清洗 `cxr_sampled_with_reports_Diagnosis_pleural_effusion_status` 列

---
1. 仅在目标列存在时清洗该列，统一大小写、首尾空格和常见分隔符写法。
2. 将常见同义表达标准化为固定状态：`positive`、`negative`、`unknown`、`missing`；对不确定值保留原值。